server:
  port: 8000


spring:
  application:
    name: truck-service
  datasource:
    url: jdbc:postgresql://localhost:5432/truck
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml



jwt:
  secret: hgtdfhfkjoytgjkygrthtrhbncvijrt
  expiresAt: 1800000

